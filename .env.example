# Example env settings
# Provide ANTHROPIC_API_KEY to use Anthropic's API (optional)
# MODEL_PROVIDER can be 'anthropic' or 'local'
ANTHROPIC_API_KEY=
MODEL_PROVIDER=anthropic
CHROMA_PERSIST_DIR=./chroma_db
EMBEDDING_MODEL=all-MiniLM-L6-v2
